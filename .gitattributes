# Normalize line endings in the repo; check out native on each platform.
* text=auto

# Shebang scripts MUST stay LF or they break on *nix (`bash: \r: command not found`).
/bin/sdb text eol=lf
*.sh text eol=lf

# Built artifact (also gitignored) — never normalize.
*.phar binary
